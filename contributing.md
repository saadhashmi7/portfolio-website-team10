this part of the project was to show conflict in file 

we did not need to do that seperatly as already when the merge was sent by branch abdul-hanan-8400 it was conflicting 

he was to write the code in his own section but edited the line which was already edited by another memeber an error was seen
CONFLICT: Merge conflict in index.html

as git can not recognise whose version it need to keep it reworte the file with some markers like these 
<<<<<<< HEAD
>>>>>>>
how this was resolved was that the file was opened that is index.html
markers were removed and kept the code that needs to be kept staged and commited the new file and pushed the new file
 
